# 🎓 教务系统爬虫 · 全自动打通 + 可视化课表 —— 工作日报

> **日期**：2026-08-07
> **作者**：张冠良（mai-feature 分支）
> **项目**：学习助手 Pro（study-helper）

---

## 一、今天做了什么（大框架）

今天完成了**两个大板块**的工作，把「学习助手」从一个纯手动的 DDL 管理工具，变成了**能自动接入南理工教务系统、自动抓取课程数据、可视化展示课表**的学习助手。

```
学习助手 Pro
├── 🕷️ 教务爬虫（纯后端 requests，全自动）
│     ├── 登录 ehall2 统一认证（密码 AES 加密）
│     ├── 访问 indexsso.jsp 建立教务会话
│     └── 抓取：课表 / 成绩 / 考试安排
│
├── 🗓️ 可视化周课表（首页）
│     ├── 按周次翻动（第1~20周）
│     ├── 星期 × 小节 网格，左侧标注上课时间
│     ├── 不同课程不同颜色块，连上多小节自动跨行
│     ├── 网课单独一行（第14节）
│     └── 考试按实际时间叠加显示
│
└── ⚙️ 配套功能
      ├── 设置页：来源切换 + 退出登录按钮
      ├── run.bat 启动自动杀旧实例
      └── 验证码人工输入桥（OCR 失败时弹图）
```

---

## 二、重点细节

### 1. 🕷️ 教务爬虫 —— 从「模板」到「全自动可用」

之前 `crawler/jwxt.py` 只是一个**未实现的模板**（`raise NotImplementedError`），今天彻底填实并跑通。

**关键突破：找到了真正的教务入口**

经过反复排查，发现南理工教务系统的认证链路并不是一开始以为的 `.113` IP 直登，而是：

```
登录 ehall2 统一认证（ids.njust.edu.cn）
  → 用会话访问 bkjw.njust.edu.cn/njlgdx/indexsso.jsp   ← 关键！
  → 拿到 MOD_AUTH_CAS cookie，自动建立教务会话
  → 之后课表/成绩/考试接口全部可访问
```

这个 `indexsso.jsp` 是门户 → 教务的 **SSO 桥**，是从门户 JS 里逆向出来的（门户点教务图标 → 调 `serviceShow` 接口 → 返回 `serviceUrl` = `indexsso.jsp`）。

**密码 AES 加密**

登录页 `encrypt.js` 里密码不是明文提交，而是：
```
64位随机前缀 + 明文密码，用页面给的 pwdEncryptSalt 做 AES-CBC 加密
```
用 Python 的 `cryptography` 库完整复刻，`_encrypt_password()` 实现。

**纯 requests 实现，家庭网络（无校园网）实测可用** —— 不需要浏览器、不需要 Edge。

### 2. 🗓️ 可视化周课表 —— 首页核心功能

课表数据来自强智教务的 `#kbtable` 图形课表 + 结构化列表，合并解析出：
- **精确小节**（如 04-05 = 第4~5小节，比"第二大节"更精确）
- **周次**（如 `5-6,8-16(周)`、`1,3,5`、单双周）
- **老师 / 教室**

**南理工作息表**（用户确认）：
| 大节 | 小节 | 开始时间 |
|---|---|---|
| 第一大节 | 第1-3节 | 08:00 / 08:50 / 09:40 |
| 第二大节 | 第4-5节 | 10:40 / 11:30 |
| 第三大节 | 第6-7节 | 14:00 / 14:50 |
| 第四大节 | 第8-10节 | 15:50 / 16:40 / 17:30 |
| 第五大节 | 第11-13节 | 19:00 / 19:50 / 20:40 |
| 网课 | 第14节 | 21:30（单独一行） |

课表页面效果：
- **◀ 上一周 / 下一周 ▶** 自由翻周
- 每门课一个**颜色块**，连上多小节用 `rowspan` 跨行
- 左侧标注每节开始时间
- **考试**（不按大节）按实际时间定位叠加（红色块）

### 3. 🚪 配套功能

- **退出登录按钮**：设置页底部，一键清除账号密码 + 课表，回到初始状态
- **同步失败自动引导**：失败时自动滚动到半自动粘贴区并高亮提示
- **run.bat 启动自动杀旧实例**：保证每次启动都是最新代码，不会跑旧版本

---

## 三、已验证的成果

- ✅ 真实账号登录成功（家庭网络）
- ✅ 课表 17 门课完整入库（精确小节 + 周次 + 老师 + 教室）
- ✅ 成绩 29 条完整解析
- ✅ 可视化课表渲染、翻周、颜色、时间标注、跨行全部正常
- ✅ 考试叠加层定位正确（用模拟考试验证：2026-12-20 09:30 → 第15周周日）
- ✅ 全部 10 个页面 200，编译通过

---

## 四、待办 / 注意

- **考试安排**：当前是假期，考试接口 0 条。开学后学校发布考试，点同步就会自动出现考试块
- **学期起始日期**：考试周次定位依赖 `semester_start`（默认 2026-09-07），若实际开学日期不同需调整（计划做成设置页可配置）
- **手机端**：纯 requests 方案天然支持手机端，后续可讨论
- **校园网直登**：`.113` 那条校内直登也已实现，回学校可验证

---

*本文件由 Claude 辅助生成，用于团队同步进度。*
